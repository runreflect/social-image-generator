{
  "name": "social-share-generator",
  "version": "0.0.1",
  "description": "Automatically generate Twitter cards / Open Graph images from Markdown front matter",
  "scripts": {
    "build": "tsc --build --clean && tsc --project tsconfig.json",
    "test": "jest"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "dependencies": {
    "gray-matter": "^4.0.3",
    "node-html-to-image": "^3.2.4"
  },
  "devDependencies": {
    "@types/node": "^18.7.6",
    "typescript": "^4.7.4"
  }
}
